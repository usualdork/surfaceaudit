id: v2-assess-risky-ftp
info:
  name: FTP Exposed
  author: surfaceaudit
  severity: medium
  tags: [assessment, risky_port]
  description: "Risky port 21 (FTP) is open"
match:
  condition: and
  matchers:
    - type: port
      ports: [21]
assess:
  category: risky_port
  severity: medium
  description: "Risky port 21 (FTP) is open"
